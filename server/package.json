{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  
  "scripts": {
    "start": "node index.js",
    "build": "cd frontend && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "\"Roger Liu\"",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  }
}
