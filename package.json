
{
  "name": "full_stack_phone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "install-client": "cd client && npm install",
    "install-server": "cd server && npm install"
  },
  "dependencies": {}
}